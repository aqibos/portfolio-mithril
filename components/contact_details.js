const m = require('mithril');

var ContactDetails = {
  controller: function(args) {
    return {};
  },

  view: function(ctrl, args) {
    return m('.contact-details', 'Contact Details');
  }
};

module.exports = ContactDetails;