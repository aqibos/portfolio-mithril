const m = require('mithril');

var ContactForm = {
  controller: function(args) {
    return {};
  },

  view: function(ctrl, args) {
    return m('.contact-form', 'Contact Form');
  }
};

module.exports = ContactForm;